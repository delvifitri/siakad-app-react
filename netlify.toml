[build]
command = "npm run build"
publish = "build/client"

# Ensure SPA client-side routing works
[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# Optional: Pass through for API/functions if added later
# [[redirects]]
# from = "/api/*"
# to = "/.netlify/functions/:splat"
# status = 200
